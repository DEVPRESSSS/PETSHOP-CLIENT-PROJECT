<UserControl x:Class="PetShop.UCpages.Products"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PetShop.UCpages" xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             Background="White"
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border x:Name="PageBorder">


        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition/>

            </Grid.RowDefinitions>

            <Border
                Background="#F4F5F6"
                BorderThickness="0,0,0,1"
                Grid.Row="0">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <!-- Left-aligned -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Right-aligned -->
                    </Grid.ColumnDefinitions>

                    <!-- Left-aligned Export Button -->
                    <TextBox x:Name="SearchName" Width="170"
                            HorizontalAlignment="Right"
                            Margin="0,0,10,0"
                            ToolTip="Search here"
                             MaxLength="30"
                            TextChanged="SearchName_TextChanged"
                            Height="25">

                    </TextBox>
                    <!-- Right-aligned New Button -->
                    <Button x:Name="ProductBtn"
                        Style="{StaticResource CrudButton}"
                        Cursor="Hand"
                        Margin="0,0,10,0"
                        Width="60"
                        BorderThickness="0"
                        Background="#FF4966"
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        Click="ProductBtn_Click">

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="New" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,5,0"
                                    Foreground="White"
                                   />
                            <Icon:PackIconMaterial 
                                x:Name="IconAdd"
                                Kind="Plus" 
                                Height="10"
                                Width="10"
                                Margin="0"
                                Foreground="White"
                                Style="{StaticResource WindowControlIcon}" />
                        </StackPanel>
                    </Button>

                </Grid>
            </Border>


            <Border Grid.Row="1">
                <!--Employee Table-->

                <DataGrid RowStyle="{DynamicResource DataGridRowStyle1}" 
                          ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
                          CellStyle="{DynamicResource DataGridCellStyle1}" 
                          x:Name="ProductTable" Style="{DynamicResource DataGridStyle1}"
                          AutoGenerateColumns="False">

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="ProductId" CanUserResize="False"  Binding="{Binding ProductId}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Name" CanUserResize="False"  Binding="{Binding ProductName}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Category" CanUserResize="False"  Binding="{Binding Category}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="OrigStock" CanUserResize="False"  Binding="{Binding Quantity}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="CurrentStock" CanUserResize="False"  Binding="{Binding CurrentStock}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Price" CanUserResize="False" Binding="{Binding Price, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Created" CanUserResize="False" Binding="{Binding DateCreated, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Width="*"/>

                        <DataGridTemplateColumn Header="Action" CanUserResize="False" IsReadOnly="True" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button x:Name="editBtn" Style="{StaticResource gridEditButton}" Click="editBtn_Click" >
                                            <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                        <Button x:Name="deleteBtn" Margin="5 0 0 0" Style="{StaticResource gridRemoveButton}" Click="deleteBtn_Click_1"
                                                
                                               >
                                            <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

            </Border>
        </Grid>


    </Border>

</UserControl>
