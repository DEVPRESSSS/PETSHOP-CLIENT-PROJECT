<UserControl x:Class="PetShop.UCpages.Employees"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PetShop.UCpages" xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
              Background="White"
             Loaded="UserControl_Loaded"
             d:DesignWidth="800">
    <Border x:Name="PageBorder">


        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition/>

            </Grid.RowDefinitions>

            <Border
                Background="#F4F5F6"
                BorderThickness="0,0,0,1"
                Grid.Row="0">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <!-- Left-aligned -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Right-aligned -->
                    </Grid.ColumnDefinitions>

                    <!-- Left-aligned Export Button -->
                    <TextBox x:Name="SearchName" Width="170"
                             HorizontalAlignment="Right"
                             Margin="0,0,10,0"
                             ToolTip="Search here"
                             MaxLength="30"
                             TextChanged="SearchName_TextChanged"
                             Height="25">
                        
                    </TextBox>
                    <!-- Right-aligned New Button -->
                    <Button x:Name="NewEmployeeBtn"
                        Style="{StaticResource CrudButton}"
                        Cursor="Hand"
                        Margin="0,0,10,0"
                        Width="60"
                        ToolTip="Add new Employee"
                        BorderThickness="0"
                        Background="#FF4966"
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        Click="NewEmployeeBtn_Click">

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="New" 
                               VerticalAlignment="Center"
                               Margin="0,0,5,0"
                                Foreground="White"
                               />
                            <Icon:PackIconMaterial 
                            x:Name="IconAdd"
                            Kind="Plus" 
                            Height="10"
                            Width="10"
                            Margin="0"
                            Foreground="White"
                            Style="{StaticResource WindowControlIcon}" />
                        </StackPanel>
                    </Button>

                </Grid>
            </Border>


            <Border Grid.Row="1">
                <!--Employee Table-->

                <DataGrid RowStyle="{DynamicResource DataGridRowStyle1}" 
                      ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
                      CellStyle="{DynamicResource DataGridCellStyle1}" 
                      x:Name="EmployeeTable" Style="{DynamicResource DataGridStyle1}"
                      AutoGenerateColumns="False">

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="EmployeeId" CanUserResize="False"  Binding="{Binding EmployeeId}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Name" CanUserResize="False"  Binding="{Binding Name}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Address" CanUserResize="False"  Binding="{Binding Address}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Phone" CanUserResize="False"  Binding="{Binding Phone}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Gnail" CanUserResize="False"  Binding="{Binding Gmail}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Created" CanUserResize="False"  Binding="{Binding DateCreated}" IsReadOnly="True" Width="*"/>

                        <DataGridTemplateColumn Header="Action" CanUserResize="False" IsReadOnly="True" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button x:Name="editBtn" Style="{StaticResource gridEditButton}" Click="editBtn_Click_1" >
                                            <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                        <Button x:Name="deleteBtn" Margin="5 0 0 0" Style="{StaticResource gridRemoveButton}" Click="deleteBtn_Click"
                                            
                                           >
                                            <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

            </Border>
        </Grid>


    </Border>
</UserControl>
